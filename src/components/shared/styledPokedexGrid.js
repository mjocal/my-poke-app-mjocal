import styled from "styled-components";

const StyledPokedexGrid = styled.div`
  display: flex;
  flex-wrap: wrap;
  align-items: flex-start;
  align-content: space-between;
  margin-bottom: 0;
  gap: 1rem;
`;

export default StyledPokedexGrid;
