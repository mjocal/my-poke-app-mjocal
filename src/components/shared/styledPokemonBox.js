import styled from "styled-components";
import { Box } from "@mui/system";

const StyledPokemonBox = styled(Box)`
  border: 1px solid;
  border-radius: 8px;
  border-color: #1976d2;
  margin: 8px auto;
  width: 15rem;
`;

export default StyledPokemonBox;
